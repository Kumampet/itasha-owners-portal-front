---
description: 仕様を壊さない実装手順と禁止事項
alwaysApply: true
---

# 実装ガイド

## 手順

1. 対象の `.cursor/docs/screens` または `features` / `api` を読む
2. `architecture/routing-and-access.md` で認証・公開範囲を確認
3. 既存コンポーネント・`src/lib` を再利用し、同パターンで変更
4. 関連 `__tests__` と `npm run check` を実行

## 禁止

- 公開パス（`/events`, `/app/groups/[id]` 閲覧, 掲載依頼, 問い合わせ）をログイン必須にしない
- `ADMIN` 専用 API を `ORGANIZER` に開放しない（既存と異なる場合は仕様更新が先）
- 承認前イベントを公開一覧・サイトマップに出さない
- HTML をサニタイズせず描画しない
- 団体メッセージを非メンバーに見せない
- 地理ブロック（451）を API だけ回避させない

## ロール・セッション

`auth.ts` と middleware の判定をずらさない。セッション項目変更は型定義と全利用箇所を同時更新。

## スコープ

依頼外のリファクタ・無関係ファイルの編集をしない。
